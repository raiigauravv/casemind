"""
Memory ablation test — Phase 6 deliverable. Not yet implemented.

Runs the same synthetic case through the agent twice: once with
memory_client disabled (no retrieval), once enabled. Captures both
reasoning outputs side by side to quantify memory's value for the demo.
"""
